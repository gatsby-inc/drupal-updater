This scripts automatically update "first" article by adding "!" to the title

Check `.env.sample` for required env vars (locally you can copy it and name it `.env` and fill those vars in)

## Install deps

```
yarn
```

## Run

```
yarn start
```
